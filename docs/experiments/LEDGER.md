# Experiment Ledger

Append-only. Newest on top. One row per experiment.

**Headline** = the single number we'd quote ("overall mean across N datasets, full_agent"); use _pending_ until the experiment finishes.
**Retro** links to the per-experiment markdown file in this directory (or a `docs/report/` file for backfilled entries).

| date       | exp_name                                  | model               | git_sha  | hypothesis                                                                                                            | headline                                          | retro                                                                  |
|------------|-------------------------------------------|---------------------|----------|------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------|------------------------------------------------------------------------|
| 2026-07-23 | `b4_retrieval_kob` (gss, cps time transfer) | none (LLM-free) | _pending_ | Is the B2 residual composition (closeable by raking sibling contexts to the target's public X-margins, reading no target Y-data) or genuine mechanism shift? | **Largely composition — and it transports, gated by retrieval breadth.** B4 transports the target's joint Y-structure (T2 assoc + T3 covariate-R^2) from **leave-one-context-out same-instrument siblings**, raked to the target's public X-margins (IPF), through the existing B1/B2 shared-latent draw — no copula surgery. Two configs: `B4_retrieval` (transported sibling R^2, **stricter firewall** than B2 — reads no target R^2) and `B4_retrieval_targetR2` (keeps B2's target R^2 — isolates the reweighting effect). **Finding 1 — the KOB composition transport beats B2 on BOTH pairs at B2's own firewall:** cps 0.663->**0.694**, gss 0.683->**0.735** (raking a sibling pool recovers composition signal the single unraked source leaves on the table; X-margins only). **Finding 2 — R^2-transport works when the pool is rich, fails when thin, exactly as ESS predicts:** cps (3 siblings, ESS 0.65) fully-firewalled `B4_retrieval` **beats even the target-R^2 config** (0.708, T3 0.753 near ceiling 0.761 — closes 94% of the B2 T3 residual reading zero target Y-data); gss (1 sibling, ESS 0.10) transported R^2 **collapses T3** 0.687->0.447, dragging B4_retrieval (0.653) under B2. **Verdict:** "irreducible mechanism shift" is rejected for the parts that transport (cps T3, composition on both) and confirmed as a **data-scarcity** problem (not mechanism-invariance) for gss — the mechanism is there to borrow, there is just one gss sibling on disk. **Slice-2 aim, sharpened:** fold raking in now (free X-margin win); the learned model's real job is robust conditional-strength transport at LOW retrieval breadth (the gss regime), with **ESS as the concrete uncertainty gate** — an ESS-gated hybrid (transported R^2 when ESS high, fall back to target R^2 when low) scores ~0.708/0.735, beating B2 on both. | [report](../report/2026-07-23-b4-retrieval-kob-transport.md) |
| 2026-07-23 | `b3_llm_prior` (gss, cps time transfer) | claude-sonnet-4.5 | _pending_ | Does the no-donor LLM prior pointed at the target (its context + codebook) close the mechanism residual that B1/B2 leave — i.e. is the prior a better structure source than transplanting the source? | **No — and that closes the gate in favour of Phase 3.** B3 = the current no-donor full method (LLM completes people from the TARGET's marginals + context, then variance-repair), scored identically to B0/B1/B2 on the crosswalk. Three rungs (raw / elicited-R^2 / pool-R^2). **On both pairs the best B3 rung sits below B1, below B2:** cps `0.599 < 0.646 < 0.663`, gss `0.615 < 0.651 < 0.683` (B3 worse than B2 by 0.064/0.068, beyond the 0.054 floor). The prior is a **weaker** structure source than transplanting the source copula. Failure modes are opposite by instrument: on **cps** the prior gets dependence ~right (T2 0.606) but the R^2 repair HURTS (T3 raw 0.386→pool 0.199) — corroborated by the durable path; on **gss** the prior nails T1 (0.80 vs B1/B2's 0.64) but **invents pairwise structure** (T2 0.43–0.49 vs 0.82, below even the independence floor's 0.72), while the repair rescues over-confident numerics (T3 0.199→0.55). Validation: B3_raw reproduces `nodonor_fullmethod` raw within 0.003 (cps 0.599 vs 0.596). **Gate: a large mechanism residual survives B2 AND B3** (best 0.663/0.683 vs ceilings 0.816/0.811, ~55–60% of B1→ceiling open) — the exact condition the roadmap names to **justify Phase 3 (learned adaptation in statistics space)**. B3 also sharpens the target: predict the X–Y **dependence** (T2) and **conditional strength** (T3, mis-set in instrument-specific directions), not marginals (T1 already solved). | [report](../report/2026-07-23-b3-llm-prior-pointed-at-target.md) |
| 2026-07-23 | `b2_recalibration` (gss, cps time transfer) | none (LLM-free) | _pending_ | Does recalibrating a borrowed generator's strengths to the target's published aggregates close B1's mechanism residual? | **Half the idea works; the other half is a measured dead end.** B2 = B1's marginal-swap draw + per-outcome R^2/dispersion recalibration from the target pool. B2 > B1 on both benchmark-backed pairs: cps 1970->1980 **0.646->0.663**, gss 1994->2018 **0.651->0.683**, with the gain concentrated where the mechanism aims -- **T3 +0.056 / +0.089** -- and T1/T2 flat. Direction is consistent **4 of 4** across source waves (Delta T3 +0.056..+0.214), though most individual overall gaps sit inside the ~0.054 noise floor. **Step A (per-pair association recalibration) failed on two vehicles and was dropped**: an explicit Gaussian copula leaked dependence (assoc error 0.114 vs B1's 0.067 on pairs it never touched, worst on nominals), and per-column coherence rates cost -0.015 overall -- too coarse (7/11 alphas fit to exactly 1.0) and directionally incapable (27/55 pairs want MORE dependence than alpha<=1 can supply). Caveats that matter: a zero-gap control gains just as much (+0.034), so B2 corrects conditional strength **generally, not transfer drift**; and much of the T3 gain came from fixing a `"No Child"` **sentinel bug** that had silently skipped 6 of 8 outcomes. The planned 5-pair sweep was **invalid** -- Layer-2 resolves its reference from the dataset name, so extra cps pairs re-scored against the same 1980 reference (cps_1970_2000 came out byte-identical to cps_1970_1980); reverted. **Gate: a large residual survives B2** (0.663/0.683 vs ceilings 0.816/0.811, ~60% of the B1->ceiling gap open) -- but the gate reads "survives B2/**B3**" and **B3 is unbuilt**, so Phase 3 is not yet justified. | [report](../report/2026-07-23-b2-aggregate-recalibration.md) |
| 2026-07-22 | `transfer_map` (gss, cps time transfer) | none (LLM-free) | _pending_ | First cross-context measurement: across same-country time transfer, where is the gap composition (cheap) vs mechanism (hard), and does the copula transfer? | **The copula travels; the levels don't.** Across GSS 1994→2018 + the CPS 1970/80/90/2000 ladder, the dependence structure is **78–100% copula-stable** and the stable fraction **decays monotonically with the time gap** (1.00 at 10y → 0.78 at 30y). But outcome shifts are almost entirely **mechanism, not composition** — only **2 of ~50** (pair×outcome) cells are composition-dominated (cps laborforce 1970→1980 0.71; gss marital_status 0.59); reweighting age/gender/race closes ~none of the socioeconomic gap (period/cohort effects, not demographic mix). This partly overturns the roadmap's "time transfer ≈ composition" prior. A **firewalled marginal-swap** (B1: source copula + target marginals, never the target's joint/test) **beats the within-target no-donor independence floor** — cps **0.649 vs 0.412** (+0.24), gss 0.625 vs 0.505 — almost entirely on T2/T3 (cps T3 0.003→0.58): the copula borrowed from another decade is worth far more than independence, exactly as its 78–100% stability predicts. B1 sits between the floor and the microdata ceiling using no target microdata. cps 3-seed/B=100; gss 1-seed preliminary. | [report](../report/2026-07-22-transfer-map.md) |
| 2026-07-20 | `gss_unblocked_acs_blocked` (gss, acs) | none (LLM-free bracket) | b818210 | Turn back to gss/acs: are they really blocked, or is the right source sitting unused (as with cps 1970->1980)? | **gss UNBLOCKED, acs genuinely blocked.** gss's blocker was the stale gss1994 wave (no wealth/mental_health); the same-year **gss2018.csv** (2348 rows) -- the wave the reference was sampled from -- has all 33 columns with data, giving a 1348-row row-disjoint pool. Bracket (B=200): floor **0.546**, ceiling **0.812** (T1 0.805 / T2 0.689 / T3 0.142 at the floor). gss's no-donor floor is 0.14 ABOVE cps's (0.407) -- entirely T2 (0.689 vs 0.353): gss is attitude-dominated (religion, politics, trust) with weak pairwise coupling, so independence loses little. The joint carries less signal where the data is closer to independent. Audit runs clean (sentinel + identity; child_number correctly NOT flagged -- GSS asks lifetime births, monotone in age 0.34->2.47, unlike cps household roster). **acs blocked, confirmed not assumed:** acs_clean.csv is the SAME 1000-person sample as the reference (identical means/sds, ~0 pool after carve) -- needs real IPUMS ACS microdata. | [report](../report/2026-07-20-gss-unblocked-acs-blocked.md) |
| 2026-07-20 | `cps_fertility_semantics` (cps) | claude-sonnet-4.5 + none | _pending_ | Chase cps's weak T3 to its root and optimize the generation. | **A variable-semantics trap, and variance repair masking it.** The benchmark's config labels `child_number` "Number of Child ever born" but the DATA is IPUMS household-roster children (own kids UNDER 18 living with the respondent): mean 0.66 (lifetime 1980 was ~2.5-3), 60+ average 0.16, profile peaks 35-45 then collapses. The LLM trusted the label -> generated lifetime fertility -> **inverted the strongest T3 relationship** (real Spearman(age,first-birth) **+0.62**, LLM produced **-0.26**), tanking T3. Correcting the DEFINITION in the prompt (public metadata, not the pool's answer distribution -> stays non-circular): honest raw **0.502 -> 0.591** (T3 0.208->0.367, T2 0.469->0.618, all >> noise floor 0.054), landing within 0.018 of the supplied-aggregate ceiling (0.609). **Bigger finding: variance repair now HURTS (0.591->0.549).** On buggy v1 it helped (mean-collapse to deflate); on corrected v2 the generation is well-calibrated (afc own R2 0.362 < real 0.420 -- already UNDER), so one-directional deflation + a too-low elicited afc target (0.200) push afc away from truth. **The old 0.502 headline was repair compensating for a generation bug.** Repair is right only where the joint is measurably over-strong, not universally. Also: hardened the generator to retry batches through the user's flaky network. | [report](../report/2026-07-20-cps-fertility-semantics.md) |
| 2026-07-17 | `scorer_noise_and_cps` (cps) | claude-sonnet-4.5 + none | 5e1aeb9 | Finish cps: re-run the no-donor method against the correct 1980 pool (the recorded 0.413 used the stale 1970 wave). | **cps done — and the scorer turned out to be the noisiest thing we measure.** cps no-donor: variance repair lifts T3 **0.031 → 0.208** (~12σ) and overall **0.439 → 0.502** (~8σ), replicating mean-collapse on a 2nd dataset; the stale-pool 0.413 is superseded. But the cfps headline does **not** port — cps loses on the joint benchmarks (T2 0.496 vs published ~0.71, T3 0.208 vs ~0.57), and a labeled pool-R² ceiling proves elicitation isn't the wall (T3 only 0.264). **The big finding: the benchmark's scorer is non-deterministic** — `np.random.default_rng()` unseeded + `bootstrap_B: 1` for T1 in *every* config. One FIXED frame scores overall 0.313–0.498, T1 0.429–1.000. σ/call: T1 0.100, T3 0.063, overall 0.043 → **a 5-seed mean can't resolve an overall gap below 0.054**. One root cause behind T4's "σ≈0.13", the 2-seed 0.950, and today's run-to-run drift. Fixed without redefining the benchmark: `insignificant_rate = not_sig/B` is a Monte Carlo estimate, so B is a replicate count, not the estimand — at B=200 means hold and σ falls as 1/√B (overall σ 0.030→0.004, 7×); `score()` now takes `seed` (bit-reproducible) + `bootstrap_B`. **Also re-measured cfps at B=200:** floor **0.458**±.004 (not 0.477/0.492 — those two "agreeing" was two noisy draws from one distribution, not a check), T4 **0.795**±.052 (σ was 0.130 — T4's "known σ≈0.13" was mostly scorer noise), ceiling 0.779; T5 moved most (0.762→0.622 at the floor), so re-check any past T5 claim. **Retracted:** "floor 0.433 beats published ~0.40" (B=200 floor = **0.407**, dead heat), "0.481 beats the 0.46 envelope" (Δ 0.02), "T1 drops under repair" (Δ 0.013 vs min-resolvable 0.126). Also found: `covariate_r2` dummied numeric predictors and used `log` not `log1p` — cps is the 1st dataset with a numeric predictor (`23dc877`). **Open:** `type2/3` mark age/gender/race `input: true` — published systems are *handed* the test's demographics while we draw them from marginals, so every "vs published" number compares unequal regimes. | [report](../report/2026-07-17-scorer-noise-and-cps.md) |
| 2026-07-17 | `port_cross_dataset` **[CORRECTIONS]** (cfps, cps, addhealth) | claude-sonnet-4.5 + none (LLM-free brackets) | 77c02e9 | Port the cfps no-donor method to other datasets and find out why they score worse. | **They don't — three of OUR measurement bugs did.** Corrected, the datasets match: microdata ceiling on T1–T3 is cps **0.827** vs cfps **0.791** (cfps all-5-types 0.808±.030, T4 **0.780**±.130 at 5 seeds — winnable, unlike addhealth's 0.000). Bugs: (1) cps ran against the stale `cps1970` wave when the same-year `cps-asec1980.csv` (181k rows) existed; (2) **shipped bug in `conditional_variance.py`** — missingness re-scattered at RANDOM, severing it from the row (`age_first_childbirth` covariate-R² 0.51→0.03); fixing it is worth **+0.31 cps T3 / +0.26 cfps T3** in the microdata regime (fixed in 77c02e9, regression test added); (3) `str.cat` propagates NaN → the carve deleted all 92k *incomplete* rows → bogus T1=0.14. **Discovery: T3 is an R²-matching test** (`type3.py:177-188` compares R², never coefficients) — retroactively validating the variance-repair design. **Retracted:** "transfer beats the LLM method" (compared microdata vs no-donor regimes — meaningless) and the cfps "integrity check". **Survives:** mean-collapse is universal (cps raw T3 0.04 → 0.31 repaired); addhealth T4 unwinnable (real microdata scores **0.000**). The missingness fix does *not* help our no-donor method (0.648→0.642, n.s.) — the LLM emits 0.022 NaN vs pool 0.274, so modelling `P(missing\|covariates)` is the next real gap. | [corrections](../report/2026-07-17-cross-dataset-corrections.md) |
| 2026-07-15 | `exp007b_discover_claude_repair` (cfps, via OpenRouter) | claude-sonnet-4.5 | d46e539 | The agents' T4=0.000 was scattered event ages, not a model limit. Turn the event-timing repair ON (joint donor draw of the 3 ages, binned on gender/education/birth_year) and the best agent should reach the ceiling. | **Confirmed: Claude agent 0.617 → 0.770** (+0.153), T4 **0.000 → 0.810**. Per-type: T1 0.848, T2 0.730, T3 0.648, T4 0.810, T5 0.812. Now = the block_donor strategy (0.79) and near the resampling ceiling (0.806). The fix: `conditional_joint_repair` now quantile-bins continuous condition keys so birth_year matches by bin instead of collapsing to the unconditional fallback. | _(this row)_ |
| 2026-07-15 | `exp007_discover_{gpt,claude,llama,gemini}` (cfps, via OpenRouter) | 4 models (see headline) | 3abd0a6 | Given `fit_block_donor` in the toolset but NOT told to use it (family-neutral `rubric_tools_v4`), does full_agent DISCOVER the donor approach, and does that generalise across model vendors? | **Discovery is real but partial and vendor-dependent.** 3/4 models used fit_block_donor unprompted; none matched the hand-built strategy (0.79) or ceiling (0.806). Overall: Claude-sonnet-4.5 **0.617** (10 blocks), gpt-5.1 0.588 (5), gemini-2.5-flash 0.548 (2 blocks+18 conditionals), llama-4-scout 0.448 (1 block, fell into the per-column marginal trap: T2 0.53/T3 0.14). **T4=0.000 for ALL four**, two mechanisms: GPT/Claude scattered the 3 event ages across separate donor blocks → order broken (child-before-marriage 31%/17% vs real 2%); Gemini ordered right (0.7%) but its permutation DISTRIBUTION (index-of-dissim ≈0.05) missed T4's razor-thin tolerance (passing strategy = 0.015). No model reproduced the strategy's tight event coupling. Also fixed: OpenRouter returns `choices:null` on upstream errors → client now surfaces the real message (llama's first run crashed on this). | _(scratchpad analysis)_ |
| 2026-07-15 | `ship_cfps_donor` **[CORRECTED]** (block-donor, cfps) | none (no LLM)  | _pending_ | the agent's bad scores are OUR bugs, not the model's: no step family preserves both conditional structure AND the marginal, `score_overall` is marginal-only, `fit_marginal` destroys missingness, a blocking commit gate is unsatisfiable on string sentinels. Fix by copying whole coherent blocks from covariate-matched real donors. | **Correction of the 2026-07-13 headline.** 12 paired seeds (was 5): block-donor does **NOT** beat resampling. cfps overall — resample **0.806**±.008, block-donor mega 0.784, domain 0.793 (PNAS 0.30). Paired mega−resample = −0.022 (t=−1.38, n.s.); no benchmark a significant win. The prior "0.819 vs 0.765" was seed noise (T4 per-seed σ≈0.15). **The microdata regime is SATURATED**: achievable ceiling (fresh real people vs benchmark) = 0.806, unbeatable in principle since resampling is an unbiased pool draw. Diagnosis of the 4 agent bugs stands; T4-winnable stands (resample 0.825, domain 0.867). Real headroom is the no-donor regime. | [saturation+disclosure](../report/2026-07-15-benchmark-saturation-and-disclosure.md) · [bugs](../report/2026-07-13-full-agent-failure-modes.md) |
| 2026-05-08 | `exp006c_tools_cross` + `exp006c_tools_long`       | gpt-5.4-2026-03-05  | 2ca14a6  | rubric_tools_v2 — family-selection recipe (cross T3) + chronology recipe (long T4) | cross: gss/cps T3 hit (0.207 → 0.485, 0.197 → 0.537), acs T3 still ~0; long: us T4 +0.095 (coincidental — recipe never fired), nlsy T3 -0.196 (agent rolled back conditionals to maximize score_overall, which is per-col-marginal only). `score_event_order` never called. → EXP-006e: hard-gate `commit_generator` on score_event_order for longitudinal. | [cross](2026-05-08-exp006c_tools_cross-report.md) [long](2026-05-08-exp006c_tools_long-report.md) |
| 2026-05-07 | `exp006b_tools_cross` + `exp006b_tools_long` (Stage B) | gpt-5.4-2026-03-05  | 6388338  | tool-using agent loop generalizes off ACS — all 7 datasets full_agent | 7/7 ran clean, beat paper-best on overall in 7/7 (+0.002 to +0.199); vs same-model baseline mixed (gss +0.086, cps −0.079, acs −0.102, nlsy ≈, addhealth −0.057, cfps ≈, us no-baseline). T3 the dominant gap; → EXP-006c. | [cross](2026-05-07-exp006b_tools_cross-report.md) [long](2026-05-07-exp006b_tools_long-report.md) |
| 2026-05-07 | `exp006a_tools_acs` (Stage A spike)       | gpt-5.4-2026-03-05  | 4bb8dcd  | tool-using agent loop matches baseline on ACS (recovers EXP-001's rubric crash) | overall=0.498 (vs baseline 0.505 in-noise; vs rubric 0.369 +0.129) | [report](2026-05-07-exp006a_tools_acs-report.md) |
| 2026-05-06 | `exp001_rubric_cross` + `exp001_rubric_long` | gpt-5.4-2026-03-05 | c4eaddd | T1-T5 rubric in SYSTEM_PROMPT lifts overall by +0.03 to +0.08 (vs `pilot_paper_agents_gpt54` / `pilot_paper_longitudinal_gpt54`) | mixed: rubric reaches LLM but pushes ambitious code that crashed 3/7 cells. us +0.174 vs paper-best, acs −0.136 vs same-model baseline. Diagnosis → EXP-006. | [reports](2026-05-07-exp001_rubric_cross-report.md) |
| 2026-05-05 | `pilot_paper_longitudinal_gpt54`          | gpt-5.4-2026-03-05  | 6d9e112  | gpt-5.4 (full) on longitudinal — does it crack T4?                                                                     | overall mean=0.453; **first ever T4>0** (US 0.27) | [report](../report/2026-05-05-three-way-model-comparison.md)            |
| 2026-05-05 | `pilot_paper_agents_gpt54`                | gpt-5.4-2026-03-05  | 6d9e112  | gpt-5.4 (full) on cross-sectional — recover from gpt-5.4-mini's T1 regression?                                         | overall mean=0.507 (vs mini 0.375; vs DS 0.563)   | [report](../report/2026-05-05-three-way-model-comparison.md)            |
| 2026-05-04 | `pilot_paper_longitudinal_gpt54mini`      | gpt-5.4-mini        | e28a4e8  | gpt-5.4-mini on longitudinal — paradigm vs T4 ceiling                                                                  | overall mean=0.367; T4≈0 (paradigm-bound)         | [report](../report/2026-05-04-gpt54mini-vs-deepseek.md)                 |
| 2026-05-04 | `pilot_paper_agents_gpt54mini`            | gpt-5.4-mini        | e28a4e8  | model swap deepseek→gpt-5.4-mini — same agent, faster + cheaper LLM                                                    | overall mean=0.375 (vs DS 0.563, big regress)     | [report](../report/2026-05-04-gpt54mini-vs-deepseek.md)                 |
| 2026-05-03 | `pilot_paper_agents_preserve`             | deepseek-v4-flash   | (early)  | preserve_missingness flag (separate prompt branch) lifts T3 from NaN                                                   | mixed; flag never fully wired, dead code remains  | [report](../report/2026-05-03-preserve-missingness-ablation.md)         |
| 2026-05-03 | `pilot_paper_longitudinal`                | deepseek-v4-flash   | a2e6a83  | extend framework to longitudinal panels with T4/T5                                                                     | overall mean=0.345; T4≈0; T3 NaN on 3/4 datasets  | [report](../report/2026-05-03-longitudinal-pilot.md)                    |
| 2026-05-03 | `pilot_paper_agents` (full paper compare) | deepseek-v4-flash   | 054781d  | rank our agent paradigm vs SSDataBench's 15 LLMs across 7 datasets × 5 metrics                                          | DS beats paper-best on 6/7 datasets (Δ +0.10)     | [report](../report/2026-05-03-paper-comparison-full.md)                 |
| 2026-05-02 | `pilot_paper_agents` (initial)            | deepseek-v4-flash   | (early)  | first paper-faithful run on real `sampled_*.csv` for GSS/CPS/ACS                                                        | overall mean=0.563 (cross-sectional only)         | [report](../report/2026-05-02-paper-faithful-pilot.md)                  |
| 2026-04-30 | `pilot_gss` / `pilot_cps` / `pilot_acs`   | deepseek-v4-flash   | (early)  | initial smoke pilots, prove the framework end-to-end                                                                   | qualitative — wiring works                        | [report](../report/2026-04-30-initial-findings.md)                      |
